# বাংলাদেশ নির্বাচন ২০২৬ - স্মার্ট ভোটিং সিস্টেম

এই প্রজেক্টটি **Next.js 14**, **Firebase** এবং **Tailwind CSS** ব্যবহার করে তৈরি একটি সম্পূর্ণ প্রোডাকশন-রেডি ভোটিং অ্যাপ্লিকেশন।

## 📁 প্রজেক্ট স্ট্রাকচার (Project Structure)
- `app/` - অ্যাপের মূল রাউটিং এবং পেজসমূহ (Next.js App Router)
- `lib/` - ফায়ারবেস কনফিগারেশন এবং হেল্পার ফাংশন
- `components/` - রিইউজেবল UI কম্পোনেন্ট
- `public/` - স্ট্যাটিক ফাইল (ছবি, manifest)
- `scripts/` - ডাটাবেস সিডিং বা মেইনটেন্স স্ক্রিপ্ট (অপশনাল)

## 🛠 সেটআপ গাইড (Setup Guide)

### ১. ডিপেন্ডেন্সি ইনস্টল করুন
টার্মিনাল বা কমান্ড প্রম্পট ওপেন করে নিচের কমান্ডটি দিন:
```bash
npm install
```

### ২. এনভায়রনমেন্ট ভেরিয়েবল সেটআপ
প্রজেক্টের রুটে `.env.local` নামে একটি ফাইল তৈরি করুন এবং নিচের তথ্যগুলো দিন (ফায়ারবেস কনসোল থেকে):

```env
# Client-Side (Firebase Project Settings > General)
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Server-Side (Firebase Project Settings > Service Accounts)
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=your_service_account_email
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n..."
# SEED_SECRET=mysecret123 (ডাটাবেস রিসেট করার জন্য)
```

### ৩. ফায়ারবেস সেটআপ (Firebase Setup)
1. **Authentication**: Firebase Console এ 'Email/Password' এবং 'Anonymous' (ভোটারদের জন্য অপশনাল) চালু করুন।
2. **Firestore Database**: 'Production Mode' এ ডাটাবেস তৈরি করুন। `firestore.rules` ফাইলটি অটোমেটিক ডেপ্লয় হবে অথবা ম্যানুয়ালি কনসোলে পেস্ট করুন।
3. **Storage**: ইমেজ আপলোডের জন্য স্টোরেজ বাকেট তৈরি করুন।

### ৪. প্রজেক্ট রান করুন
```bash
npm run dev
```

## 🚀 ডাটাবেস তৈরি (Seeding)
প্রথমে ৩০০টি আসন তৈরি করতে ব্রাউজারে হিট করুন:
`http://localhost:3000/api/seed?secret=YOUR_SECRET`
(নোট: `.env.local` এ `SEED_SECRET` সেট থাকতে হবে অথবা কোড থেকে চেক সরাতে হবে)

## 🛡️ অ্যাডমিন প্যানেল (Admin Panel)
ইউআরএল: `/admin`
লগইন পেজে আপনার ফায়ারবেস অথেন্টিকেটেড ইমেইল পাসওয়ার্ড দিয়ে লগইন করুন।

## 📦 Vercel এ ডেপ্লয়মেন্ট (Deployment)
1. GitHub এ কোড পুশ করুন।
2. Vercel এ নতুন প্রজেক্ট ইম্পোর্ট করুন।
3. **Environment Variables** সেকশনে `.env.local` এর সব ভ্যালু কপি করে পেস্ট করুন।
4. `FIREBASE_PRIVATE_KEY` এর ডাবল কোট বা নিউলাইন ঠিকভাবে সেট করুন (Vercel এ বিশেষ করে)।
5. **Deploy** ক্লিক করুন।

## ✨ ফিচারসমূহ (Features)
- **Secure Voting**: ফিঙ্গারপ্রিন্ট এবং আইপি ডিটেকশন।
- **Real-time Admin**: লাইভ ড্যাশবোর্ড এবং চার্ট।
- **Responsive**: মোবাইল ফ্রেন্ডলি এবং PWA সাপোর্ট।
